# Slim runtime deps for the hosted Streamlit dashboard (Streamlit Community Cloud).
# This is intentionally minimal — the dashboard never imports the offline-pipeline
# libraries (soccerdata, statsbombpy, optuna, statsmodels, matplotlib, etc.), so
# they're excluded to keep the cloud build fast and reliable.
# For full local development + the data/backtest pipeline, use requirements-dev.txt.

streamlit>=1.30
altair>=5.0
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
lightgbm>=4.0
pyarrow>=14.0
pyyaml>=6.0
joblib>=1.3
requests>=2.31
