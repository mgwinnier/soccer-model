# Full local/dev + offline-pipeline dependencies (NOT used by the hosted dashboard).
# The deployed Streamlit app uses the slim root requirements.txt instead.
# Install locally with:  pip install -r requirements-dev.txt

# Core scientific stack
pandas>=2.0
numpy>=1.24
scipy>=1.10
statsmodels>=0.14
scikit-learn>=1.3

# Gradient boosting
lightgbm>=4.0

# Data IO / scraping
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
pyarrow>=14.0
pyyaml>=6.0

# Utilities / viz
tqdm>=4.66
joblib>=1.3
matplotlib>=3.7

# Live odds, xG data, and dashboard
soccerdata>=1.8      # FBref scraping (xG/style) — often IP/CAPTCHA-blocked
statsbombpy>=1.11    # StatsBomb open data (xG)
streamlit>=1.30      # dashboard UI
altair>=5.0          # dashboard charts

# Hyperparameter tuning (optional — GBM falls back to defaults if absent)
optuna>=3.4

# Dev / testing
pytest>=7.4

# TLS via OS cert store (corporate/UniFi proxies); harmless/optional on Linux
truststore>=0.9
